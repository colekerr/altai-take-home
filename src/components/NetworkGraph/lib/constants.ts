export const SVG_DIMENSION = 500 as const;

export const SVG_CENTER = SVG_DIMENSION / 2;

export const SVG_NODE_RADIUS = 15 as const; // in pixels

export const SVG_EDGE_RADIUS = SVG_CENTER - SVG_NODE_RADIUS;

export const TOTAL_CIRCLE_RADIANS = 2 * Math.PI;
